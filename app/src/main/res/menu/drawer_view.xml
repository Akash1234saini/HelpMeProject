<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- it ensure that which item is selected -->
    <group android:checkableBehavior="single">

         <!-- Navigation menu -->
        <item android:id="@+id/home"
            android:title="@string/home"
            android:icon="@drawable/ic_menu_home"/>

        <item android:id="@+id/emergencyNum"
            android:title="@string/emergencyNumbers"
            android:icon="@drawable/ic_menu_call"/>

        <item android:id="@+id/saved"
            android:title="@string/saved"
            android:icon="@drawable/ic_menu_check_circle"/>

        <item android:id="@+id/history"
            android:title="@string/history"
            android:icon="@drawable/ic_menu_history"/>

        <item android:id="@+id/setting"
            android:title="@string/setting"
            android:icon="@drawable/ic_menu_setting"/>

        <item android:id="@+id/about"
            android:title="@string/about"
            android:icon="@drawable/ic_menu_about"/>

    </group>

</menu>